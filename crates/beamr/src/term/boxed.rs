/// Boxed term headers and accessors.
///
/// Each boxed value on the heap starts with a header word encoding
/// the type and size, followed by the payload. This module defines
/// the header format and provides typed accessors for each boxed
/// variant: tuple, list cons, float, big integer, closure, map,
/// and reference.

pub(crate) fn _scaffold() {}
