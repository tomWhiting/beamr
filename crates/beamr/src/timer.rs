//! Timer wheel for timeouts.
//!
//! Handles `receive after` timeouts, periodic ticks, and step
//! deadline enforcement. Wakes suspended processes when their
//! timeout fires. O(1) insertion and cancellation via hierarchical
//! timer wheel buckets.

pub(crate) fn _scaffold() {}
