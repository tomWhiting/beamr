//! Process name registry.
//!
//! Allows processes to be registered under atom names so they can
//! be found without tracking PIDs directly. Used by supervisors
//! and system services. A name maps to exactly one PID; registering
//! an already-taken name fails.

pub(crate) fn _scaffold() {}
