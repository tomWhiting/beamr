[package]
name = "beamr-cli"
version = "0.1.0"
edition = "2024"
description = "Thin runner for beamr — load a .beam file and execute it"

[[bin]]
name = "beamr"
path = "src/main.rs"

[dependencies]
beamr = { path = "../beamr" }
