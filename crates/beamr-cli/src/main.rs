/// Beamr CLI — thin runner for bring-up.
///
/// Point at a .beam file, optionally register native functions,
/// execute a specified function, and print the result. Primary
/// value is during development: watch the VM work before Meridian
/// is wired in. May become disposable once in-process hosting
/// via norn is operational.
fn main() {
    todo!("beamr-cli: load .beam, register natives, run")
}
