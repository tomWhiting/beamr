{
  "type": "object",
  "properties": {
    "requirements": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {"type": "string", "description": "The requirement ID (e.g. R1)"},
          "fields_present": {"type": "array", "items": {"type": "string"}, "description": "List of field names present on this requirement object"}
        },
        "required": ["id", "fields_present"]
      },
      "description": "For each requirement, the id and list of all field names present."
    },
    "merge_evidence": {"type": "string", "description": "Did any requirement have fields beyond the original brief fields (id, title, spec, acceptance, files, checklist, stories)? Name the extra fields if so."}
  },
  "required": ["requirements", "merge_evidence"]
}
