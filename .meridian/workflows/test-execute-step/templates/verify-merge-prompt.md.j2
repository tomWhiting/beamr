Below is the current state of input.brief.requirements AFTER the scout merge. Report exactly what you see — for each requirement, list the id and every field name present on it. Do not infer, do not add, just report the raw data.

```json
{{ input.brief.requirements | tojson }}
```
