{
  "design_system": "yggdrasil-briefs",
  "version": "0.1.0",
  "scope": "private",
  "profiles": ["sonnet-minimal"],
  "input": {
    "type": "object",
    "properties": {
      "items": {
        "type": "string",
        "description": "JSON array of items with id and name fields."
      }
    },
    "required": ["items"]
  },
  "trigger": {
    "kind": "manual"
  }
}
