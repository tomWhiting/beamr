{
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {"type": "string"},
          "fields": {"type": "array", "items": {"type": "string"}, "description": "Every field name present on this item"}
        },
        "required": ["id", "fields"]
      }
    },
    "has_color": {"type": "boolean", "description": "true if ANY item has a 'color' field"},
    "raw_dump": {"type": "string", "description": "The raw JSON string you were given, verbatim"}
  },
  "required": ["items", "has_color", "raw_dump"]
}
