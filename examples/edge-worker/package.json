{
  "name": "beamr-edge-worker-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers example for serving HTTP requests with a preloaded Beamr WASM bundle.",
  "scripts": {
    "dev": "wrangler dev --local",
    "test": "node --test test/worker.test.mjs"
  },
  "devDependencies": {
    "miniflare": "^4.20260609.0",
    "wrangler": "^4.20.0"
  }
}
