---
type: brief
id: B-032
cluster: beamr
title: gleam_otp supervisor support — gleam_otp_external + end-to-end test
---

# B-032: gleam_otp supervisor support — gleam_otp_external + end-to-end test

> **Cluster:** beamr
> **Depends on:** B-030, B-031
> **Checklist:**
> - C150 — BIF registry maps (Module, Function, Arity) to a Rust function pointer
> **Stories:**
> - S6 (Workflow Author, Writing Gleam Workflows) — As a workflow author, I want to define supervision trees over my workflow steps so that failed steps are automatically restarted according to a declared strategy.
> - S8 (Meridian Runtime, Embedding beamr for Workflow Execution) — As the Meridian runtime, I want to create a beamr VM instance, load .beam modules, and spawn processes from Rust so that I can embed workflow execution in the server process.

## Purpose

Implement gleam_otp_external stubs, fill remaining BIF gaps, and prove supervision works end-to-end with a Gleam supervisor + worker test.

## Task

Stub gleam_otp_external, fill BIF gaps, write end-to-end supervisor test.

## Requirements

### R1: gleam_otp_external stubs

application_stopped/0 returns ok. supervisor:start_link/2 returns {ok, self()}.

**Acceptance:**
- application_stopped() returns ok
- supervisor:start_link/2 returns {ok, Pid}
- No unresolved imports for gleam_otp modules

**Files:**
- create: crates/beamr/src/native/otp_stubs.rs
- modify: crates/beamr/src/native/mod.rs
- modify: crates/beamr-cli/src/main.rs

**Checklist:**
- C150 — BIF registry maps (Module, Function, Arity) to a Rust function pointer

**Stories:**
- S8 (Meridian Runtime, Embedding beamr for Workflow Execution) — As the Meridian runtime, I want to create a beamr VM instance, load .beam modules, and spawn processes from Rust so that I can embed workflow execution in the server process.

### R2: Remaining BIF gaps

Implement erlang:++/2, length/1, pid_to_list/1, get/0, and any other BIFs discovered during integration.

**Acceptance:**
- All gleam_otp BIFs execute correctly
- No unresolved imports
- Tests cover each new BIF

**Files:**
- modify: crates/beamr/src/native/gate3_bifs/mod.rs

**Checklist:**
- C150 — BIF registry maps (Module, Function, Arity) to a Rust function pointer

**Stories:**
- S8 (Meridian Runtime, Embedding beamr for Workflow Execution) — As the Meridian runtime, I want to create a beamr VM instance, load .beam modules, and spawn processes from Rust so that I can embed workflow execution in the server process.

### R3: End-to-end supervisor integration test

Compile a Gleam supervisor+worker, load all modules, start supervisor, verify worker runs, crash worker, verify restart.

**Acceptance:**
- Supervisor starts successfully
- Worker processes messages
- Crash triggers restart
- Restarted worker handles messages
- Runs on beamr scheduler

**Files:**
- create: crates/beamr/tests/supervisor_e2e.rs
- create: crates/beamr/tests/fixtures/otp_e2e/

**Checklist:**
- C150 — BIF registry maps (Module, Function, Arity) to a Rust function pointer

**Stories:**
- S6 (Workflow Author, Writing Gleam Workflows) — As a workflow author, I want to define supervision trees over my workflow steps so that failed steps are automatically restarted according to a declared strategy.

## Boundaries

- SHALL NOT implement full OTP supervisor module
- SHALL NOT implement OTP application controllers
- SHALL NOT implement hot code upgrade
