---
type: brief
id: B-040b
cluster: beamr
title: Gleam stdlib I/O + URI wrappers and sample workflow e2e
---

# B-040b: Gleam stdlib I/O + URI wrappers and sample workflow e2e

> **Cluster:** beamr
> **Depends on:** B-040a
> **Checklist:**
> - C150 — BIF registry maps (Module, Function, Arity) to a Rust function pointer

## Purpose

Implement remaining 13 gleam_stdlib BIFs (I/O, utility, URI), plus uri_string stubs, and un-ignore sample_workflow_e2e test to prove end-to-end Gleam workflow execution.

## Task

Implement 13 gleam_stdlib BIFs, 2 uri_string stubs, and verify sample workflow e2e.

## Requirements

### R1: gleam_stdlib I/O and utility wrappers (7 functions)

Register: is_null/1, list/5, map_get/2, print/1, print_error/1, println/1, println_error/1.

**Acceptance:**
- is_null(nil) returns true
- is_null(42) returns false
- map_get(#{a => 1}, a) returns {ok, 1}
- map_get(#{a => 1}, b) returns {error, nil}
- print/println write to configured IoSink
- Tests cover each function

**Files:**
- modify: crates/beamr/src/native/stdlib_stubs/gleam_stdlib_ffi2.rs
- modify: crates/beamr/src/native/stdlib_stubs/mod.rs

**Checklist:**
- C150 — BIF registry maps (Module, Function, Arity) to a Rust function pointer

### R2: URI wrappers + uri_string stubs (6 functions)

Register gleam_stdlib: parse_query/1, percent_decode/1, percent_encode/1, uri_parse/1. Register uri_string: parse/1, dissect_query/1. Approximate stubs.

**Acceptance:**
- percent_encode(<<"hello world">>) returns <<"hello%20world">>
- percent_decode(<<"hello%20world">>) returns <<"hello world">>
- uri_string:parse(<<"https://example.com/path?q=1">>) returns map with scheme, host, path, query
- Tests cover basic cases

**Files:**
- create: crates/beamr/src/native/stdlib_stubs/uri_bifs.rs
- modify: crates/beamr/src/native/stdlib_stubs/mod.rs

**Checklist:**
- C150 — BIF registry maps (Module, Function, Arity) to a Rust function pointer

### R3: sample_workflow e2e test — un-ignore and verify

Remove #[ignore] from sample_workflow_run_completes_end_to_end. Verify end-to-end.

**Acceptance:**
- Test loads all .beam files from test-workflows/sample/
- sample_workflow:run/1 completes with ExitReason::Normal
- Return value is a well-formed tuple
- Test passes without #[ignore]

**Files:**
- modify: crates/beamr/tests/sample_workflow_e2e.rs

**Checklist:**
- C150 — BIF registry maps (Module, Function, Arity) to a Rust function pointer

## Boundaries

- SHALL NOT implement full RFC 3986 URI parsing — stub with approximate splitting
- SHALL stub uri_string functions as approximations
- SHALL mark approximate stubs with inline notes for future replacement
